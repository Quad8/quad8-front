import classNames from 'classnames/bind';
import styles from './SwitchHelp.module.scss';

const cn = classNames.bind(styles);

export default function SwitchHelp() {
  return <div className={cn('wrapper')}>test</div>;
}
