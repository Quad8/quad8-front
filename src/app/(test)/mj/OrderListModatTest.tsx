'use client';

import classNames from 'classnames/bind';
import styles from './ReviewModalTest.module.scss';

const cn = classNames.bind(styles);

export default function OrderListModalTest() {
  return <div className={cn('container')} />;
}
