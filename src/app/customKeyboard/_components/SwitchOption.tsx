import classNames from 'classnames/bind';
import styles from './SwitchOption.module.scss';

const cn = classNames.bind(styles);

export default function SwitchOption() {
  return <div className={cn('wrapper')}>keycap</div>;
}
